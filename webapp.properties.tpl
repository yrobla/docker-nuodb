# the port for the webapp
webapp.port = 48080
webapp.appWar = nuowebapp.war
webapp.wsWar = nuowebservice.war
domain.broker = ${HOST}
domain.admin = ${DOMAIN_USER}
domain.password = ${DOMAIN_PASSWORD}
